{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.ts",
  "scripts": {
    "deploy": "npm run build:ui && npm run build && rm -rf viceav && mkdir viceav && cp -r out dist package.json .env viceav",
    "build": "npx tsc",
    "start": "node out/index.js",
    "dev": "npx ts-node-dev src/index.ts",
    "test": "env NODE_ENV=test npx tsx --test tests/**/*.test.ts",
    "build:ui": "rm -rf dist && cd ../frontend && npm run build && cp -r dist ../backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@eslint/js": "^9.37.0",
    "@tsconfig/recommended": "^1.0.10",
    "@types/bcrypt": "^6.0.0",
    "@types/express": "^5.0.3",
    "@types/jsonwebtoken": "^9.0.10",
    "@types/supertest": "^6.0.3",
    "eslint": "^9.37.0",
    "json-server": "^1.0.0-beta.3",
    "supertest": "^7.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.0"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.1"
  }
}
